#include "my.h"

char			*my_strcat(char *dest, const char *src)
{
    unsigned int	i;
    unsigned int	len;

    i = 0;
    len = my_strlen(dest);
    while (src[i])
        dest[len + i] = src[i++];
    dest[len + i] = '\0';
    return (dest);
}
